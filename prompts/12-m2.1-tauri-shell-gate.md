# Prompt 12 — M2.1: Tauri shell gate — IPC + compositing → Tauri-go / CEF decision

> Use with `00-orchestrator.md` (v2). **Effort `max`** — this is ADR-003's hard exit gate; it can force a CEF pivot, so it leads M2 (risk-first, the M0 playbook). **Must run on Windows** (WebView2 is the binding target; ubuntu CI can't see this).
> **Lane:** Owns a throwaway `/spikes/tauri-shell` · Blocked by nothing (independent of `/core`) · **Parallel-safe with M2.2 (13) and M1.6 (11)** · see `prompts/README.md`. Throwaway spike — measurements over polish.
> Read first: ADR-003, and the **research correction below**.

---

<task>
Resolve ADR-003's M2 exit gate on Windows WebView2 with TWO co-equal sub-gates, and make the Tauri-go-vs-CEF call on evidence.
**Research correction (verified June 2026):** ADR-003's "~200 ms / 10 MB" floor is a *large-payload bandwidth* figure (~50 MB/s on Windows), **not** a per-frame budget — Metrocalk's wire carries small coalesced deltas, so the IPC sub-gate will likely pass. The *real* risk — the one **Graphite actually left Tauri over** — is **compositing a transparent webview UI over a native wgpu viewport**, blocked by the OS compositors. That is Metrocalk's exact architecture, so it is a first-class gate here, co-equal with IPC.
</task>

<scope>
In scope (`/spikes/tauri-shell`, Windows): (1a) the IPC delta-wire benchmark; (1b) the transparent-webview-over-wgpu compositing test; the go/CEF decision → ADR. Out of scope: the real editor UI, the production transport impls (M2 build, post-gate), rendering a real scene (that's M2.2 — a triangle suffices here).
</scope>

<deliverables>
1. **Sub-gate 1a — IPC delta wire.** Drive a synthetic 60 Hz drag carrying the *real coalesced-delta encoding* (from the transport trait) through: a **Tauri Channel** (Rust→JS), **`invoke` raw-binary** (JS→Rust commit), and a **local-WebSocket** variant. Measure per-frame over ≥60 s: end-to-end delta latency (p50/p95/p99), IPC time-budget consumed, frame-interval jitter / dropped frames, the real delta payload-size distribution, and a 1 KB→1 MB single-payload sweep to locate the Windows MB/s ceiling.
2. **Sub-gate 1b — compositing (the Graphite-killer).** Build the real shell shape: a native **wgpu surface** (a triangle is fine) with the React UI as a **transparent-region webview composited over it** in one window — Tauri v2 child-webview / `unstable` (ref `FabianLars/tauri-v2-wgpu`). Over a sustained session + window resize + drag, observe: flicker / z-fighting, whether transparent regions actually composite (scene shows through), input routing (clicks hit the right layer), DPI/fullscreen stability. Windows first; note Linux/X11 separately (expected weakest — a Linux-only failure is a known-risk fast-follow, not a Tauri-killer).
3. **The decision → ADR.** Tauri-go iff **both** sub-gates pass on Windows; else CEF — re-framed as the *compositing* escape hatch (offscreen-render the UI to a texture + self-composite, Graphite's proven path), not an IPC one. Verso stays "revisit-when-mature," not a fallback.
</deliverables>

<success_criteria>
**1a PASS:** sustained 60 Hz with **p99 end-to-end delta latency ≤ ~4 ms** AND **p99 IPC budget ≤ ~5 ms** (≤ ⅓ of 16.6 ms), **zero dropped frames** over 60 s with the real encoding; per-frame payload stays well under the measured Windows ceiling. **1b PASS:** flicker-free, correctly composited, correct input routing on Windows for a sustained session. Decision recorded with quoted numbers; ADR written (ADR-003 confirmed, or superseded with the CEF pivot).
</success_criteria>

<verification>
Both sub-gates measured on **real Windows WebView2** — pin + record the WebView2 runtime version + hardware. 1a: two runs (median + p99). 1b: capture before/after screenshots or video of the composited window under motion/resize. Adversarial paragraph: "the case this passes on the dev box but stutters on min-spec" — test representative min-spec or state the caveat explicitly.
</verification>

<definition_of_done>
☐ `/spikes/tauri-shell` builds + runs on Windows, one documented command · ☐ 1a latency/jitter/bandwidth table (2 runs) · ☐ 1b compositing observations + visual evidence (Windows; Linux noted) · ☐ Tauri-go / CEF decision with quoted numbers · ☐ ADR written (ADR-003 confirmed or superseded) · ☐ progress.md + architecture.md open-questions (Tauri IPC **and** the new compositing finding) updated · ☐ committed.
