# Prompt 05 — M1.1: Monorepo skeleton

The M0 gate review split M1 into five focused sessions (M1.1–M1.5); this is M1.1. The rest are `06-m1.2-wrapper-api`, `07-m1.3-registry`, `08-m1.4-stress-scene`, `09-m1.5-query-ci-gate`. (Renamed to `05-m1.1-monorepo.md` to match the `06`–`09` M1.2–M1.5 prompt filenames.)
>
> Use with `00-orchestrator.md` (v2) as system prompt. **Effort `high`.** First M1 session — production-quality code; the spike bar no longer applies. Run **before M1.2–M1.5**.
> Read first: `architecture.md` (repo layout + the 5 invariants), `progress.md` (the M1 breakdown), `M0-gate-review.md` (what M1 stands on), ADR-001/002/003/**006**.

---

<task>
Stand up the permanent cargo workspace and the CI that every later M1 session builds inside. After this session, work happens in a real monorepo with the invariants mechanically guarded and the wasm-tripwire still green — not in throwaway spike dirs.
</task>

<scope>
In scope: the workspace + crate skeletons per architecture.md (`/core`, `/transport`, `/plugins`, `/editor`, `/spikes` retained), one shared lint config, a trivial passing `/core` test, and CI that builds/tests the workspace and keeps the wasm-tripwire green.
Out of scope: the wrapper API (M1.2), registry (M1.3), any real engine logic, UI, rendering, transport impls. Crates are skeletons with a doc-comment charter and no premature design. Do NOT port spike code — spikes stay where they are.
</scope>

<deliverables>
1. **Cargo workspace** at repo root. Members: `/core` (lib), `/transport` (lib — trait sketch only, deltas-only per invariant 2), `/plugins` (lib stub). `/editor` is React/TS, **not** a cargo member — leave a README stating that (or a minimal Tauri shell stub if trivial; keep it light). `/spikes/*` retained; if they slow the default build, exclude them from the workspace default-members and document why.
2. **Shared workspace lints** in one place: `clippy::pedantic` (tuned + documented) and `unsafe_code = "forbid"` workspace-wide **except** the future ecs-wrapper crate (M1.2) — record the exception mechanism now (e.g. crate-level allow) so M1.2 doesn't fight it.
3. **One trivial `/core` unit test** that runs and passes (proves the harness end-to-end).
4. **CI**: fmt + clippy + `cargo test` across the workspace, AND the existing `wasm-tripwire` job kept green. The tripwire builds **loro + wgpu** for `wasm32` (both proven in spike ③); per **ADR-006** it must **NOT** attempt to build `flecs_ecs` or any native-only crate for `wasm32` — keep the wasm build target to the crates that actually cross.
</deliverables>

<success_criteria>
`cargo build` + `cargo test` green across the workspace · clippy/fmt clean · wasm-tripwire green and explicitly excludes flecs/native-only crates from the wasm32 build · one `/core` test passes · clean-clone build works from the documented steps only.
</success_criteria>

<verification>
Clean-clone test: fresh clone → documented setup → green build + tests + tripwire, no undocumented steps. Re-read the diff against invariants 2 and 5 — even in skeletons, don't set a precedent of full-state boundaries or leaked backend types.
</verification>

<definition_of_done>
☐ workspace builds + tests green · ☐ clippy/fmt clean · ☐ wasm-tripwire green, flecs/native-only excluded from wasm32 · ☐ shared lint config + the unsafe-forbid exception path for the wrapper documented · ☐ clean-clone test passed · ☐ progress.md Now/Next advanced to M1.2 + dated log entry · ☐ architecture.md repo section updated from "planned" to actual · ☐ working tree clean, committed.
</definition_of_done>
