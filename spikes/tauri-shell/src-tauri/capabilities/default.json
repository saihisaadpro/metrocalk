{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spike capabilities — core defaults; custom commands are allowed by default in v2.",
  "windows": ["main", "*"],
  "permissions": ["core:default"]
}
