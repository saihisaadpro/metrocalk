[package]
name = "metrocalk-animation"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Deterministic, engine-agnostic animation domain kernel for Metrocalk"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
bincode = "1"
serde_json = "1"
