//! `metrocalk-plugins` — the Extism WASM plugin host + SDK surface, and the MCP server seam for
//! the AI layer (plan section 2). Phase 2+; out of M1 scope. Stub only — no host, no SDK, no code
//! yet, just the crate so the workspace shape matches `architecture.md`.
