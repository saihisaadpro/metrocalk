# Progress — M2 (build)

Detailed session log for milestone M2. Newest first, one entry per session, measured numbers + ADR
links. Dashboard state lives in the root `progress.md`.

---

## 2026-06-16 — M2 CLOSE: reload-persistence + zero-IPC drag + measured go/no-go → M2 complete

The interactive close, all green on `master`/`origin/main` (`88bdca7` → `22975a8`). M2 is **complete (code +
all measurable gates); north-star #1 is real** (4/5 boxes human-confirmed live, box 5 wired + headless-proven).

**Delivered:**
- **Pick fix (`88bdca7`):** viewport click sent `clientX*devicePixelRatio` ÷ physical width → the ray
  missed the cloud on any scaled/HiDPI display (clicks selected nothing). Now a **normalized [0,1]
  cursor** mapped straight to NDC — DPI/offset-free. One IPC per click (not the hot path).
- **Live reload-persistence (`3182038`, box 5):** `persist.rs` — re-seed deterministically (same SEED →
  identical `EntityId`s) + replay an append-only `Edit`/`Bind`/`Undo` log through the commit pipeline,
  then `clear_history` (restored scene non-undoable). **Not** Loro merge-on-start (merge drops the ECS
  capability pairs the reveal needs). 4 headless tests: a bind survives a fresh seed+replay; seed
  determinism (the foundation); undo-nets-out; divergence-skipped (the adversarial guard).
- **Zero-per-frame-IPC drag (`61fe72e`, inv. 4):** right-drag orbit + wheel zoom update natively in the
  render loop (poll `window.cursor_position()`); JS sends only `drag_start`/`drag_end` (2×/gesture).
  `render::IPC_CALLS` reports ipc/frame so the zero-per-frame claim is measurable live. True by
  construction (no `invoke` on the per-frame path).

**Measured — M2 go/no-go (release, ≥2 runs; i9-13900H / Iris Xe iGPU / Win11 26200; rustc 1.92.0):**

| Path | p50 | p99 | Verdict |
|---|---|---|---|
| Viewport render-submit @5k (M2.6) | 0.74 ms | ≤2.5 ms | ≪16 ms ✓ |
| Live reveal (query + greyed scan) @5k | 1.25 ms | 1.52 ms | ≪16 ms ✓ |
| Commit (field edit through pipeline) @5k | ~0.4 ms | ~1.1–1.6 ms | ≪16 ms ✓ |
| `project_full` whole-scene delta @5k | ~70 ms | ~103–125 ms | one-shot (connect/undo); incremental-undo follow-up |
| Snapshot-merge-load @5k (~531 KB) | ~330–374 ms | ~372–400 ms | one-shot load; why persistence uses seed+replay |

**Interactive frame budget HOLDS** (render + reveal + commit ≪16 ms). The two heavy ops are one-shot
(connect/undo/load), not the 60 Hz path.

**Six residuals:** snapshot-load cliff **measured** (~350 ms, above); Channel-e2e **cited** (~3.4 ms,
M2.1/ADR-007 — live re-confirm needs a driver); min-spec **partial signal** (the budget holds on the
integrated Iris Xe iGPU); **handed off** (human/hardware/driver): DPI 100↔200, ≥60 s flicker, full
min-spec sweep, real-browser store-apply.

**Handed off (never fabricated):** the dogfood verdict (the felt win); the 30-s live close→reopen +
drag-feel spot-checks; the human/hardware/driver residuals above.

---

## 2026-06-15 — M2.6 live shell: the editor is real (composite + real core verified; round-trip/residuals partial)

**The first clickable Metrocalk build runs.** `/editor-shell/src-tauri` is a Tauri 2 app: a transparent
WebView2 editor UI over a native wgpu viewport on one HWND, OS-composited (ADR-008) — and it's **live
on the real `/core`, no MockCore**. Captures + numbers below. Committed on `master` (`84d997c`,
`5da4a74`), pushed.

**Architecture.** The `!Send` Flecs `Engine` lives on a dedicated **engine thread** (M2.1's finding —
kept out of Tauri's `Send+Sync` managed state), fed editor `EditTx`s over `invoke` and pushing
`ProjectionDelta`s back over a Tauri **`Channel`** (the desktop binding of the M2.4 transport
contract). The viewport render loop (M2.2's instanced path on the Tauri surface) reads a shared
`SceneState` the engine thread fills from `/core` `Transform` components; camera + **CPU ray-pick stay
in Rust** (invariant 4). Only the committed delta crosses the boundary (invariant 2).

**Measured / verified (RTX 4060 Vulkan; integrated live app):**
| check | result |
|---|---|
| Single-window composite, no blackout (ADR-008 holds at integration) | **PASS** — Desktop-Duplication: 2k scene black 0.1% / 461 colors; **5k** black 0.1% / 943 colors (`evidence/m26-*.png`) |
| Real scene renders from `/core` | **PASS** — 5000 entities → instanced cubes + grid from their `Transform`s |
| Real `/core` connected over the transport | **PASS (live)** — `connect`→`project_full`→Channel→editor shows the **real 5000 entities + ids** (the projection path round-trips through the real `invoke`+`Channel` plumbing) |
| Viewport frame budget @ 5k | **PASS** — CPU-submit p50 **0.74 ms** / p99 **≤2.5 ms** (≪16.6 ms); GPU ~0.6 ms (M2.2's identical instanced renderer) |
| Edit/bind/undo + reject-with-reason logic | **PASS (headless)** — 6 `editor-shell` tests through the real `Engine<FlecsWorld>` (`apply_edit`/`project_full`) |

**NOT yet verified — honest gaps (no fabrication):**
- **Live click round-trip in the running window** (click cube → inspector → edit → echo → viewport move; Ctrl-Z): the *plumbing* is live-proven (the projection path round-trips over the real Channel/invoke) and the edit/pick/undo *logic* is unit-tested through the real engine, but a human-click (or a WebView2-CDP driver) confirming the full interactive loop in the window is **pending**.
- **Zero-per-frame-IPC drag:** camera-drag input isn't yet routed natively — the transparent webview captures pointer events, so true zero-IPC drag needs the viewport region made click-through to the native HWND (the M2.3 input-routing applied at the OS level). **Unbuilt** — the key remaining invariant-4 work.
- **Frontend:** the shell uses a **focused UI on the M2.5 `ProjectionDelta`/`EditTx` contract**, not the full M2.5 React editor mounted verbatim (same contract; that's a packaging follow-up).
- **The six residuals:** DPI 100↔200, min-spec, sustained ≥60 s flicker watch — **human/hardware-gated**; real Channel e2e latency, Windows snapshot-load cliff, real-browser store-apply — need a **WebView2-CDP / Playwright driver** (not set up). None measured this session.

**M2 go/no-go:** the *measurable* gate is **green** (composite + real-core integration + 5k frame budget all verified live); the *interactive* gate (live round-trip, drag) + the residuals are **pending a human/driver/hardware pass**. **M2 is therefore NOT declared fully complete** — it's "integrated + render-gate-passed, interactive/residual verification pending."

**Next.** The interactive verification pass (human or WebView2-CDP driver) + native input routing for the drag + the residual measurements close M2; then M3.1 (binding-by-intent).

## 2026-06-15 — M2.6 started: consolidated to master + the real-core edit-bridge spine

**Consolidation landed on `master`.** Fast-forwarded `master` to the verified `m2-integration`
(M1.6 + all five M2 lanes), pushed to `origin/main` (`cbd4a04..ed1b9c0`). `master` now carries the
whole M2 build; CI runs the consolidated tree.

**M2.6 spine — the real-core edit bridge (no MockCore).** New workspace-member crate
`editor-shell` (`bridge.rs`): the convergence seam between the **real** `metrocalk_core::Engine`
(commit pipeline + `World` + Loro) and the M2.5 editor's `ProjectionDelta` / `EditTx` JSON contract
(camelCase, byte-identical wire shape to `editor/src/transport/protocol.ts`). `apply_edit` translates
an editor `EditTx` → a `/core` `Op` → `Engine::commit` → the echoed `ProjectionDelta`: **confirm** on
success, **reject with the pipeline's real reason** on failure ("every 'no' explained"). This is
deliverables 3–4's core proven end-to-end through the authoritative engine.

**Verified — 4 tests (real `Engine<FlecsWorld>`, no mock):** setField round-trips and the authoritative
core updates, then **Ctrl-Z undoes** via the engine-side stack (M1.6); a bind to an unknown entity is
rejected with a non-empty reason; a valid bind confirms; the EditTx/ProjectionDelta JSON matches the
editor's camelCase shape. clippy `-D warnings` + fmt clean; workspace builds with `editor-shell` added.

**NOT done — M2 is NOT complete (the gate is unmeasured).** Remaining M2.6 work, none fabricated:
- The **Tauri `/editor-shell` app**: transparent WebView2 (the M2.5 editor) over the wgpu viewport
  rendering M2.2's instanced scene (ADR-008 single-window); camera + **pixel-picking in Rust**; the
  real **Tauri Channel** (M2.4) wiring + the **Loro→`ProjectionDelta` projector** for the initial
  scene (needs a `/core` entity-enumeration read API — not yet present).
- The **live round-trip** click→inspect→edit/bind→commit→echo in the running app; **zero-per-frame-IPC
  drag** instrumentation; **wasm32 parity**.
- **Deliverable 6 residuals** (each to be measured, not assumed): DPI 100↔200 move · min-spec ·
  sustained ≥60 s flicker watch · real Channel e2e latency · Windows snapshot-load cliff · the O(n)
  store-apply re-measured in a real browser.
- The **measured M2 go/no-go** (end-to-end render + commit + delta + UI-update budget) + the heavy doc
  close (status → M2 complete, architecture diagram annotation, plan §7).

**Next.** Build the Tauri `/editor-shell` on `master` around this verified bridge, measure the
residuals + the M2 gate, then flip the milestone.

## 2026-06-14 — M2 integration: lanes M2.1–M2.5 assembled (M2.6 build pending)

**What happened.** Assembled the five M2 lanes onto branch `m2-integration` (off the M1.6 base): a
6-way merge of `m2.4` (transport) + `m2.2` (render) + `m2.3` (shell) + `m2.5` (editor) + `m2.1` (Tauri
gate). Code merged with **zero conflicts** (lanes own disjoint paths — `/transport`+`core/producer`,
`spikes/render-scene`, `spikes/shell-composite`, `/editor`, `spikes/tauri-shell`); only the shared docs
(`architecture.md`, `progress.md`, `progress/M2.md`) and the root `Cargo.toml` `exclude` line
conflicted, resolved by hand (one `exclude` covering all six spikes; docs reconciled).

**Verified.** Integrated workspace **`cargo test --workspace` green** — the one failure
(`undo_delete_entity_latency`, an M1.6 timing benchmark, untouched by the merge) **passes in
isolation**; it flaked under just-finished cold-build load. Both CI grep gates green
(loro-outside-`/core`, flecs-outside-`/ecs`). ADRs 001–010 all present. Editor JS source is
byte-identical to M2.5's 11-green tree (no lane touched `/editor`).

**Not done — M2 is NOT complete.** M2.6 (the real `/editor-shell`: composite the editor over the wgpu
viewport, wire the transport, prove the click→inspect→edit/bind→commit→echo round-trip with a measured
frame budget) is the remaining M2-close work and was **not built** this session. The one real
integration gap to bridge in M2.6: `/core` emits Loro-`update` bytes while the M2.5 editor consumes
`ProjectionDelta`s — M2.6 adds the decode seam (the core decodes Loro→projection and sends projection
deltas, per ADR-006's "browser is Loro-authoritative"). `master` remains at M1.5 (M1.6 unmerged).

**Next.** M2.6 shell↔viewport integration (build `/editor-shell` on this branch), then flip status →
M2 complete → M3.

## 2026-06-14 — M2.4 transport protocol + delta wire

**What landed.** The deltas-only transport layer (invariant 2): the wire protocol, the
backend-agnostic `DeltaTransport` trait, three impls, and the `/core` producer hook that turns each
committed transaction into a coalesced delta. Owns `/transport` + `core/src/producer.rs`.

**Design (ADR-009).** Adopted **Loro Syncing Protocol v1 framing** carrying **Loro's own
`export(update, from)` bytes as the opaque payload** — no bespoke per-op codec, no bare-Loro-without-
envelope. Causality (ordering/idempotency/out-of-order) comes from Loro's version vectors for free;
the same wire serves Phase-2 collab unchanged.

- **`/transport` links no Loro** — payloads are `&[u8]`; the Loro `export`/`import` is confined to
  the `/core` producer hook. CI `loro`-outside-`/core` grep simulated green.
- **Envelope:** 16-byte header — magic `frame_kind` (`%LOR` doc-delta · `%ACK` · `%HSK` handshake ·
  `%FRG` fragment · `%EPH` ephemeral **reserved** for Phase-2 · `%PNG`/`%PNT`) · `proto_version` ·
  room id · payload-len. `DocUpdate` = batch id + N length-prefixed update blobs.
- **Shared `DeltaSession` (written once above the byte trait):** frame-tick **coalescing** via
  version-vector diff (`updates_since(last_acked_vv)` → one blob/tick); batch-id/**ACK**;
  **outbox-collapse backpressure** (≤1 DocUpdate in flight; the gap accumulates and the next send is
  ONE spanning update, never N queued); **fragmentation/reassembly** (>256 KiB, for initial
  catch-up); a **reconciliation hook** (Wait-for-Ack, instant-ack in M2, drop-in server-reconcile
  later — kept strictly separate from the engine-side *user* undo stack, M1.6 / ADR-002 F2).
- **3 impls:** in-process (zero-copy sync loopback; browser is Loro-authoritative) · Tauri Channel
  (raw bytes, NOT JSON, over `Channel<InvokeResponseBody>` — sink injected by the shell at M2.6) ·
  local WebSocket (`ws` feature, `tungstenite`, no-TLS loopback; fallback + collab foundation).
- **`/core` producer hook:** `DeltaSource`/`DeltaSink` on `Engine` (export/import+merge); `ProducerHook`
  with `on_tick`/`pump`; **peer identity** (engine's Loro PeerID, ADR-002 F3) established in the
  handshake before any delta flows.

**Verification — 15 tests green.**
- `/transport` (10): envelope round-trip + error cases; fragment reassembly **in-order + out-of-order**
  + timeout sweep; **backpressure-collapse** (stall → 1 frame; ack → 1 spanning update, not N);
  ACK clears the unacknowledged marker; **`%EPH` reserved** (round-trips, ignored, not acked); ping→pong;
  in-process send→apply→ack.
- `/core` (5, two real Loro engines): producer-hook end-to-end convergence (5 commits → 1 coalesced
  frame, both ends learn PeerID); **out-of-order** import (apply update2 before update1 → pending →
  converges); **idempotent** re-import (no-op); **reconnect resync** from a stale vv (one update spans
  the gap); optimistic-echo **no-op** (`repaired == false`).

**Numbers (Win11 26200 · i9-13900H · rustc 1.92.0 · 2 runs each).**

| path | p50 | p99 | note |
|---|---|---|---|
| Channel envelope encode+decode (256 B coalesced delta) | 0.7 µs | 0.9–1.3 µs | ≈0.001 ms — negligible vs 16.6 ms |
| WebSocket loopback round-trip (256 B) | 1.5–3.4 ms | 2.0–3.8 ms | poll-loop-bound (1 ms reader sleep), an upper bound, NOT the wire |

**Decision.** ADR-009 (transport protocol = Loro-Protocol-v1 framing + opaque Loro-update payload).

**Honest boundaries / blockers.**
- **Tauri Channel e2e + Windows snapshot-load deferred to M2.6** — both need the real Tauri shell,
  which is on the unmerged `m2.1` branch. The Channel impl + envelope micro-bench stand alone now; the
  e2e numbers are NOT invented. (Decision made with the user: build impl, defer e2e.)
- The WS latency is dominated by the foundation's polling reader (1 ms sleep) and swings run-to-run;
  the authoritative WS IPC figure is M2.1's 1.3 ms. An event-driven reader removes the poll floor.
- The **correction** reconciliation path (`repaired == true`) is surfaced but not exercised
  end-to-end here (forcing a merge-repair is covered by `core/tests/merge.rs`); the Phase-2
  undo→apply→redo server reconcile is a drop-in `Reconciler`.
- No real collab server / concurrent remote ops yet (Phase 2) — single-process, instant-ack.

**Next.** M2.5 editor UI (consumes this transport contract) → M2.6 convergence (wire the real
Channel, run the deferred Windows benchmarks, integrate the M2.2 viewport + M2.3 shell).
M2 is risk-first (the M0 playbook): the two exit gates — **M2.1** Tauri WebView2 IPC and **M2.2**
real-scene render — run before the M2 build (real shell + transport impls + binary delta protocol),
which is authored from their verdicts.

---

## 2026-06-14 — M2.2 real-scene render gate: **PASS**

**What happened.** Built `spikes/render-scene` (throwaway, workspace-excluded like the M0 spikes):
the M1.4 stress scene rendered through one wgpu 29.0.3 crate on native + browser, GPU frame time and
CPU submit measured separately. Resolves the architecture open-question "real-scene render cost at
≥5k entities" (M0 spike ③ only proved a *triangle*).

**Approach (the task's verified research findings, all implemented):**
- **Instancing** — entity cubes + per-entity 3-axis gizmos are each ONE instanced draw; the ground
  grid is a **render bundle** (stable content). Draw-call count is constant (3) by construction.
- **GPU frustum culling → indirect** — a compute pass tests each instance's bounding sphere vs the 6
  frustum planes (Gribb–Hartmann from view-proj), compacts survivors into `visible[]`, and
  `atomicAdd`s the count into a counter copied into the draw-args. Then ONE `draw_indexed_indirect`
  (cubes) + ONE `draw_indirect` (gizmos) — designed **without** multi-draw-indirect and **without**
  `indirect-first-instance` (`first_instance=0`; the vertex shader indexes `visible[]` by
  `instance_index`). Built against `Limits::downlevel_defaults()` (storage-buffer ceiling = 128 MB,
  exactly the browser limit) to prove portability.
- **Timing** — `TIMESTAMP_QUERY` at pass boundaries (portable WebGPU; not the native-only
  `TIMESTAMP_QUERY_INSIDE_ENCODERS`), double-buffered non-blocking readback; CPU submit = wall-clock
  around encode+submit. Scene transforms from the same SplitMix64 seed as M1.4.

**Numbers (full tables → `spikes/render-scene/RESULTS.md`).** RTX 4060 / Vulkan native; Chrome 149 +
Edge 149 (Dawn) browser; 2 runs/discipline.

| discipline | 5k GPU p99 | 20k GPU p99 | budget | result |
|---|---|---|---|---|
| Native (Vulkan) | 0.60 ms | 0.88–0.95 ms | 8.3 / 16.6 ms | ✅ ~14×/17× headroom, no spikes |
| Chrome (Dawn) | 1.34 ms | 3.26 ms | 16.6 ms / 30 fps | ✅ ~750 / ~305 fps-equiv |
| Edge (Dawn) | 1.34 ms | 3.27 ms | 16.6 ms / 30 fps | ✅ |

- **Instancing sanity:** draw calls = **3** at both 5k and 20k; native GPU time grows only **1.4×**
  for a **4×** entity increase (sublinear ⇒ instancing engaged, not a linear non-instanced path).
- **Worst-case frame:** camera path captured ~100% visible (4999/5000, 19988/20000) every run.
- **Storage:** largest buffer 0.15 MB @5k / **0.61 MB @20k** ≪ 128 MB ceiling.
- **Run-to-run swing:** GPU p99 +1.5% (5k) / +7.6% (20k) — under the 25% investigate-threshold.

**Decision.** ADR-003 status-updated: real-scene render gate (M2.2) PASSED; the "non-bindless web
path from day 1" is confirmed sufficient for the editor scene.

**Blocker / gap (parked, low risk).** Firefox 141 leg **not run** — Firefox is not installed on this
machine (M0's winget copy is gone). Firefox WebGPU is built on the same `wgpu`, and the spike uses
only the portable subset (no multi-draw-indirect, `first_instance=0`, downlevel limits), so risk is
low; a Gecko pass (manual, or a non-CDP driver) is the fast follow-up. Recorded in RESULTS.md §5.

**Next.** M2.1 Tauri WebView2 IPC gate (the remaining M2 exit gate). Then author the M2 build from
both verdicts.
## 2026-06-14 — M2.3 shell-composition gate: **single-window PASS** (M2.1 1b was a GDI artifact)

**What happened.** Built `spikes/shell-composite` (throwaway, seeded from M2.1's `tauri-shell`) and
resolved how the editor shell composites the React UI over the native wgpu viewport on Windows →
[ADR-008](../decisions/008-shell-composition.md). **Verdict: single-window is the shell** (transparent
WebView2 over a native wgpu surface on one HWND). M2.1's 1b "compositing FAIL" was a **GDI capture
artifact**, now disproven.

**Why M2.1 was wrong, and how this held a higher bar.** M2.1 captured 1b with GDI `CopyFromScreen`,
which **cannot see a GPU flip-model/overlay swapchain** → the live wgpu layer read as all-black. This
spike captured with a **DXGI Desktop Duplication** tool (`capture/`) that grabs the DWM's final
composited output (overlays included). Under it the composite is clean; the M2.1 "16×16 collapse" never
reproduced (window stable 1016×739 over 12 s).

**Battery (single-window, Desktop-Duplication + visual), on 2 GPUs:**

| gate | dGPU RTX 4060 | iGPU Iris Xe |
|---|---|---|
| Real UI composited (panels over viewport) | PASS — black 0%, distinct 64–112 | PASS — black 0%, viewport ~74% |
| Transparent regions show viewport | PASS | PASS |
| Correct z-order | PASS | PASS |
| No blackout / no 16×16 collapse | PASS (stable 1016×739, black 0%) | PASS |
| No flicker under motion (rotating triangle) | PASS (viewport 71.6–73.8% across frames) | PASS |
| Survives rapid resize | PASS (700/900/1240 px) | n/t |
| Input routing under overlap | PASS (viewport clicks logged) | PASS |
| DPI 100%↔200% monitor move | not covered (caveat) | not covered (caveat) |

Deliverables: (1) **input-routing layer** built first (`input-routing/`, dependency-free, **7 unit
tests green**) — path-agnostic per-pixel UI/viewport hit test, carries to any shell; (2) single-window
confirmed with the **real React panels** (toolbar + inspector + transparent viewport + overlapping
floating box + input log); (3) **fallback ladder not exercised** (single-window passed, so no DComp /
no CEF — ~170 MB avoided); (4) ADR-008 written, ADR-007 reconciliation recorded.

**Decision.** ADR-008 = single-window. No CEF/DComp. ADR-007's compositing-FAIL consequence withdrawn.

**Honest boundaries / blockers.**
- Capture was automated swapchain-aware Desktop Duplication + frame inspection, not a human watching
  an uninterrupted 60 s session; clean motion frames sampled over ~10 s stretches (window-minimize
  hygiene interrupted longer runs) — black stayed 0%, viewport varied every clean frame (no flicker).
- **DPI 100%↔200% monitor move** and a **truly low-end machine** not covered — the two adversarial
  cases held open. The iGPU (the driver-sensitive case M2.1 feared) **was** covered and passed.
- **ADR-007 lives on the unmerged `m2.1` branch**, so its status-line note ("1b was a GDI capture
  artifact — see ADR-008") can't be edited from this lane; apply it at the m2.1↔m2.3 merge.

**Next.** M2.6 convergence: build the real single-window shell (transparent WebView2 over the wgpu
viewport), wire the `shell-input-routing` layer + the M2.4 transport, integrate the M2.2 render
verdict; run the DPI/min-spec cases there.
## 2026-06-14 — M2.5 editor UI scaffold (projection of the core)

**What landed.** `/editor` — the editor front-end as a **projection of the authoritative core**
(invariant 1), delta-fed over the M2.4 transport. Owns `/editor` (not a cargo member). React 19 +
TypeScript + Vite; Zustand · JSON Forms · React Flow · Vitest. ADR-010.

- **Projection store** (Zustand/`useSyncExternalStore`): entity-keyed, immutable per-entity, with a
  separate `{id,name,parentId}` **summary projection** for the tree. `displayed = base ⊕ pending`;
  `displayed[id]` stays ref-identical to `base[id]` when no pending op touches it.
- **Transport client**: TS mirror of the M2.4 Loro-Protocol-v1 envelope (`frame.ts`) + `DeltaClient`
  with separate `%LOR` committed-delta and `%EPH` ephemeral paths; in-process binding + desktop stub
  (M2.6 → Tauri Channel). `MockCore` stands in for the WASM core (swapped M2.6).
- **Inspector**: JSON Forms (over RJSF) with custom renderers via testers (color, `entity-ref`
  bind-target, enum); a field edit → optimistic update + a **JSON-Patch transaction** (AI layer's
  language).
- **Binding graph**: React Flow, neighborhood-scoped (Sigma.js noted for 50k+). **Hierarchy**:
  virtualized over the summary projection (5k). **Reconciliation**: optimistic echo; **rejection is
  first-class UX** ("every 'no' explained"). **Input ownership**: chrome-only for React; viewport
  events left for the native layer (invariant 4), wired in M2.6.

**Verification — 11 Vitest tests green:**
- **Selective subscription at 5k**: editing one entity re-renders exactly its subscribed detail and
  **0 of 5000** hierarchy rows; a name change re-renders exactly that one row.
- **Tear-free** under a React 19 concurrent transition (atomic multi-field delta never half-applied).
- **Reject path**: incompatible optimistic bind reverted + the core's reason surfaced; compatible bind
  confirmed. Optimistic field edit reconciles to a no-op on confirm.
- Envelope round-trip (TS mirror matches M2.4 frame.rs); app end-to-end wiring (5k scene projected
  through client→store→panels, inspector renders on select).

**Numbers.** Single-entity field-edit apply+render ≈ **24–70 ms at 5k in jsdom** (varies with machine
load). The apply is an O(n) shallow map copy (≈ tens of ms at 5k); only the **1** subscribed component
re-renders (0 of 5000 rows). The selective-subscription property is exact; jsdom is slower than a real
browser. A structural-sharing store is the noted upgrade (ADR-010) if browser profiling demands it.
(Fixed an O(n²) bulk-load bug found by the app test — `applyDelta` now copies each map once per delta.)

**Decision.** ADR-010 (editor projection architecture: Zustand-as-projection · JSON Forms over RJSF ·
React Flow neighborhood · optimistic echo with rejection-as-UX · JSON-Patch edit language).

**Honest boundaries.** Viewport hot-input → native layer is **stubbed** (wired in M2.6); `MockCore` →
real WASM/Rust core in M2.6; no real assets; AI/describe-to-create + Rules UI deferred (they reuse the
JSON-Patch `EditTx`). Responsiveness measured in **jsdom**, not a real browser (the selective-render
property is exact either way). The O(n) per-delta map copy is acceptable for the milestone; flagged for
a structural-sharing upgrade.

**Next.** M2.6 convergence: mount this UI in the M2.3 single-window shell, point the desktop transport
binding at the real Tauri Channel (M2.4) + swap `MockCore` for the WASM core, wire the viewport input
hand-off, and integrate the M2.2 render verdict.
# M2 — progress log

Newest first. Milestone dashboard: `../progress.md`. Previous milestone: `M1.md`.

---

### 2026-06-14 (M2.1 — Tauri/WebView2 exit gate: IPC + compositing → ADR-007)

Risk-first M2 entry (the M0 playbook): resolve ADR-003's deferred Windows-WebView2 gate before the
M2 build commits to a shell. Throwaway spike `spikes/tauri-shell/` (standalone Tauri v2 app, its own
workspace; not a member of the root workspace). Two co-equal sub-gates on **real** WebView2
**149.0.4022.69** (Tauri 2.11.2 / wry 0.55.1, wgpu 29.0.3 Vulkan, RTX 4060 laptop). Evidence +
machine-readable results in `spikes/tauri-shell/evidence/`.

- **Sub-gate 1a — IPC delta wire: PASS.** The wire carries the **real** coalesced-delta encoding
  (Loro update bytes from `core::Engine`, a 60 Hz transform drag through the real commit pipeline),
  raw-binary, over three transports. Two 60 s / 3600-frame runs each:
  - Tauri **Channel** (engine→UI push): p99 **3.4–3.6 ms** RTT, **0 dropped**.
  - local **WebSocket** (CEF-style): p99 **1.3–1.7 ms** RTT, 0–1 dropped.
  - `invoke` raw-binary (UI→engine): p99 6.7–6.8 ms RTT (a **~4 ms fixed per-call floor** — use it
    for episodic commits, not the 60 Hz stream).
  - Real delta = **103 bytes**; bandwidth sweep tops at ~31.8 MB/s. **Overhead-bound, not
    bandwidth-bound** → ADR-003's "~200 ms / 10 MB" worst case never applies to a deltas-only wire.
- **Sub-gate 1b — compositing: FAIL (direct single-window approach).** A transparent WebView2 *does*
  composite on Windows (control with wgpu disabled: the desktop shows through the transparent
  viewport). But attaching a native **wgpu surface to the same window HWND** blacks out and collapses
  the window — the webview UI that renders fine on its own disappears. Reproduces Graphite's exact
  conflict (their issue #2541; Tauri #9220/#11944). Not tested: Tauri `unstable` child-webview /
  DComp layering; a human eyes-on pass; min-spec hardware. (See `evidence/audit.md` for the threats
  to validity — chiefly that GDI can't capture GPU overlays, which the control narrows but doesn't
  fully eliminate.)
- **Decision → [ADR-007](../decisions/007-m2.1-tauri-gate-result.md):** IPC leg of ADR-003 confirmed
  (and its bandwidth framing corrected); the one-window wgpu-under-webview composition does not work
  on Windows → plan the shell around **self-composite** (UI-as-texture in the wgpu pass, Graphite's
  proven path), with a cheap child-webview/DComp follow-up spike before any full CEF pivot. Tauri is
  **not** disqualified (IPC passes, transparency works) — but the viewport↔UI composition must be
  self-managed.
- **Spike runs with one command** per mode: `GATE_MODE=bench|composite|selftest pnpm tauri dev`
  (see `spikes/tauri-shell/README.md`). Built + measured on Windows; `cargo`/`tauri dev` green.
